export enum SgRecordStatus {
    INIT = "init",
    PROCESSING = "processing",
    SUCCESS = "success",
    FAILED = "failed",
}

export enum VendorType {
    ALIYUN = "aliyun",
    DEEPSEEK = "deepseek",
    OTHER = "other",
}

export enum ApiFormat {
    OPENAI = "openai",
    ANTHROPIC = "anthropic",
}
